<div class="modal-dialog" role="document">
  <div class="modal-content">
    <div class="modal-header">
      <h5 class="modal-title" id="exampleModalLabel">Editar Task</h5>
      <%= button_to("Excluir", delete_card_path(@card), :method => :delete, :class => 'btn btn-info btn-sm margin5right') %>
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
    <div class="modal-body">
	    <div>
        <p class="teste"></p>
        <%= simple_form_for @card, url: edit_card_path(), wrapper: :inline_form  do |form| %>
          <p>Editar Task: </p>
          <p><%= form.label :Titulo %>
          <%= form.input_field :title %> </p>
          <p style="display: flex;"><%= form.label :descrição %>
          <%= form.input_field :description %> </p>
          <p><%= form.label :Data_Termino %>
          <%= form.input_field :dateFinish %> </p>
          <p><%= form.submit "Salvar" %></p>
        <% end %>

      </div>
    </div>
  </div>
</div>